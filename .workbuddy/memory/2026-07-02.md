# 2026-07-02 工作日志

## mem0 改为主动查询工具方案

### 完成内容
- 分析了当前 mem0 集成状态：DynamicContextMiddleware 每轮 search mem0 注入，backend=mem0，pgvector 向量库
- 设计新方案：mem0 从"被动每轮注入"改为"Agent 主动按需查询"工具，DynamicContextMiddleware 回退 file 模式
- 确定双轨制设计：memory.json 被动注入基础上下文 + mem0 工具主动精准查询
- 设计了 memory_search 工具的使用边界（正例4种 + 反例3种 + "不确定就查"原则）
- 解决了双写问题：MemoryUpdater 同时写 file 和 mem0
- 撰写了详细修改提示词

### 产出文件
- `docs/mem0-tool-refactor-prompt.md` — 完整修改提示词，含8个文件的改动指令

### 关键设计决策
- mem0_tool_enabled 与 backend 独立：可 backend=file 同时 mem0_tool_enabled=true
- 工具的 user_id/agent_id 从 LangGraph config 自动注入，LLM 只传 query
- DynamicContextMiddleware 删除所有 mem0 代码，回到原 file 逻辑
- MemoryUpdater 双写：file 给被动注入，mem0 给主动查询
- 需修改 mem0_client.py 的 is_mem0_enabled()，让 tool 模式也能初始化 mem0

## 完善修改提示词

### 完成内容
- 完善了 mem0-tool-refactor-prompt.md，添加更多实现细节
- 新增内容：配置矩阵（4种 backend×tool 组合）、mem0 返回格式兼容（3种格式）、get_config() 异常处理、score 显示、独立 try/except 双写、防滥用三层防线、SubAgent 不注册工具的决策、回滚方案表
- 每个文件的修改都给出了完整的"原代码→替换为"对比
- 总改动量从 110 行更新为 210 行（新增 150 + 修改 60）

# 2026-06-27 工作记录

## 前端页面重新优化（git 回退后重做）
- 因用户 git 回退了之前的改动，重新对 17 个文件进行了完整的视觉/交互优化
- 所有功能逻辑保持不变，Next.js 生产构建验证通过

### 修改文件清单
1. `globals.css` - 自定义滚动条、动画、工具类
2. `tailwind.config.ts` - 阴影系统、动画关键帧
3. `login/page.tsx` - 渐变背景、品牌标识、密码可见切换
4. `register/page.tsx` - 同 login 风格统一
5. `Sidebar.tsx` - 品牌头部、活跃状态高亮、Settings 图标
6. `TopBar.tsx` - 头像、双行信息、退出按钮
7. `(dashboard)/page.tsx` - 功能卡片引导页
8. `MessageItem.tsx` - react-markdown 渲染 AI 消息
9. `MessageList.tsx` - 空状态引导、流式指示器优化
10. `InputBar.tsx` - textarea 自动高度
11. `ToolCallCard.tsx` - React.memo + 视觉统一
12. `SubAgentCard.tsx` - React.memo + 状态图标
13. `TokenUsageBar.tsx` - React.memo + 可视化条
14. `AgentNode.tsx` - 选中态 ring 高亮
15. `NodePalette.tsx` - hover 阴影
16. `ConfigPanel.tsx` - 分组标签、滑块刻度
17. `CanvasControls.tsx` - 圆角 + group hover
18. `MonitoringPanel.tsx` - 统计卡片图标
19. `threads/[thread_id]/page.tsx` - Tab 栏优化
20. `settings/page.tsx` - Tab 图标、工具卡片高亮
21. `start.sh` - 添加 `rm -rf .next` 清理缓存

## Markdown 渲染深度优化
- 安装了 `@tailwindcss/typography` (之前 prose 类因插件缺失完全不生效)
- 安装了 `@types/react-syntax-highlighter` (TypeScript 类型)
- `MessageItem.tsx` 重写：抛弃 `prose` 类，改为自定义 react-markdown `components` 映射
  - 代码块：`react-syntax-highlighter` + oneDark 主题，带语言标签头部
  - 表格：边框 + 斑马条纹 + 全响应式 overflow
  - 引用块：蓝色左边框 + 斜体
  - 列表：正确缩进 + 间距
  - 标题：分层大小 + 二级分隔线
  - 链接：下划线 + hover 加深
  - 图片：圆角 + 自适应宽度
- `tailwind.config.ts` 加入 `@tailwindcss/typography` 插件

## Bug 修复
1. `crypto.randomUUID` 安全上下文报错 — `utils.ts` 增加 `getRandomValues` 降级
2. SSE 连接失败无错误提示 — `ChatPanel.tsx` + `CanvasControls.tsx` 增加连接失败错误横幅
3. 产经构建 vs 运行时性能 — `next/dynamic` → `React.lazy` + `Suspense` 避免 SSR bootstrap 报错

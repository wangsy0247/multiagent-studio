# 2026-07-15 工作日志

## 项目简历更新（基于项目最新修改）
- 项目有重大更新，重新全面分析后端和前端
- harness/ 从 133→151 个 Python 文件（~34K LOC），app/ 从 31→32 个
- 新增 Team 多 Agent 协作模式（核心新特性，~2000 LOC）：
  - TeamOrchestrator（~990 LOC）：四阶段事件驱动编排 PLANNING→SPAWN→DISPATCH_LOOP→SYNTHESIS
  - TeammateAgent（~975 LOC）：持久化 Agent，状态机 SPAWNING→IDLE↔WORKING→SHUTDOWN
  - TeamTaskStore：JSON + fcntl.flock + DAG 依赖解析 + DFS 循环检测
  - TeamMessageBus：per-agent JSONL inbox + drain-on-read + asyncio.Event
  - 15 个 Team 工具（Lead 6 / 共享 5 / Member 4）
  - 自主认领三级优先级（强制分配/饥饿预防/领域匹配）
  - 结构化协议握手（shutdown_request/response + plan_approval）
  - Watchdog：超时 30min / 死锁 2min / 循环依赖 DFS
- 新增中间件：SandboxAuditMiddleware[6]、SafetyFinishReasonMiddleware[18]、DeferredToolFilterMiddleware[15]
- 三层配置系统 L0/L1/L2（系统默认→用户全局→Per-Agent）
- Skills 系统：.skill ZIP 安装 + 安全扫描 + JSONL 版本管理 + 回滚
- MCP 集成：持久化 session pool + OAuth + mtime cache
- 前端 Team UI 完整实现：5 列任务看板 + 团队消息流 + 成员状态 + @mention
- Agent Team 短期记忆（最新 Git 提交：4a0ccd1）
- 重新生成 resume_project.html，从 10 个亮点扩展到 12 个，新增 Team 协作为核心亮点

## 项目简历更新（后端聚焦版）
- 用户要求"主要介绍后端的部分"，重新生成以后端为核心的简历
- 确认最新数据：harness/ 151 文件 31,539 LOC，app/ 32 文件 2,789 LOC
- 移除前端可视化亮点，新增"SubAgent 守护线程隔离执行引擎"作为亮点 5
- 新增后端模块划分卡片（12 个模块目录）
- SSE 亮点从"前后端"改为纯后端视角（事件生成机制、跨线程桥接）
- 最终成果数据聚焦后端指标
- 保留 12 个技术亮点，全部以后端实现为核心
